export enum EFilterType {
  category = 0,
  color,
  storage,
  subBuild,
}

export enum EDisplayGroup {
  Alpha = 0,
  Color,
  Category,
  Storage
}

export enum EFileOption {
  Load = 0,
  Save = 1
}

export enum EDisplayMode {
  TV = 1,
  CV = 2
}
